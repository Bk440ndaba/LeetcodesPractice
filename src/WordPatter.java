import java.util.HashMap;

public class WordPatter {
    public boolean wordPattern(String pattern, String s) {
        
        //The approach is this, If my map already contains the key, and the value mapped to the key is NOT equal
        // to the current value in the iteration, return false

    }   
}
